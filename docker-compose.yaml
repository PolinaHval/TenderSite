version: "3"
services:
  postgres:
    image: "postgres:13.4-alpine"
    container_name: "main"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "tender_site"
    ports:
      - "35432:5432"